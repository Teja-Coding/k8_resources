apiVersion: v1
kind: ConfigMap
metadata:
  name: podconfig
data:
  database_url: "jdbc:mysql://mysql-service:3306/mydb"
  app_environment: "production"
  log_level: "INFO"
  config.json: |
    {
      "featureFlags": {
        "newFeature": true,
        "betaTesting": false
      },
      "apiEndpoints": {
        "users": "/api/v1/users",
        "products": "/api/v1/products"
      }
    }
